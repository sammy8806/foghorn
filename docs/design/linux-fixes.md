# To Fix For Linux

- [x] Fix popup auto-position so the window opens at the top-right on Linux instead of near the top-left.
- [ ] Replace the macOS-only tray implementation with a Linux-capable tray backend.
- [x] Make `url` actions open with a Linux-compatible launcher instead of `open`.
- [x] Make `clipboard` actions use a Linux-compatible clipboard implementation instead of `pbcopy`.
- [ ] Verify Linux packaging/runtime prerequisites for Wails desktop builds and document them.
